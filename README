In order to run the program, you need to have two files: usermap.txt, which is the output of the cat /etc/passwd command,
						    and file_ls, which is the output of the ls -la –time-style=full-iso command
